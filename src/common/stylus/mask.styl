.mask {
  width: 100vw;
  height: 100vh;
  background: rgba(135, 206, 250, 0.5);
  position: fixed;
  left 0
  top 0
  .box {
    width: 500px;
    height: 300px;
    background: #FFF68F;
    padding 20px;
    box-sizing border-box
    position: fixed;
    left: 50%;
    margin-left: -250px;
    top: 50%;
    margin-top: -150px;
    
  }
}